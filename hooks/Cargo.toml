[package]
name = "carina_hooks"
version = "0.1.0"
authors = ["lholznagel <github@lholznagel.info>"]

[dependencies]
futures-cpupool = "0.1.8"

clippy = { version = "0.0.195", optional = true }

[features]
default = []
dev = ["clippy"]