[package]
authors = ["lholznagel <contact@lholznagel.info>"]
name = "blockchain_protocol"
version = "0.1.0"

[dependencies]
crc = "1.7.0"
time = "0.1.38"
blockchain_hooks = { path = "../hooks" }

[dev-dependencies]
quickcheck = "0.6.0"
