sudo: false

language: rust

notifications:
  email: false

cache: 
  directories:
  - $HOME/libsodium
  - $HOME/.cargo

rust:
  - stable
  - beta
  - nightly

install:
  - wget https://github.com/jedisct1/libsodium/releases/download/1.0.16/libsodium-1.0.16.tar.gz
  - tar xvfz libsodium-1.0.16.tar.gz
  - cd libsodium-1.0.16 && ./configure --prefix=$HOME/libsodium && make && make install && cd ..
  - export PKG_CONFIG_PATH=$HOME/libsodium/lib/pkgconfig:$PKG_CONFIG_PATH
  - export LD_LIBRARY_PATH=$HOME/libsodium/lib:$LD_LIBRARY_PATH